{% extends 'base.html.twig' %}

{% block title %} Modifier {{ pokemon.nom }}{% endblock %}

{% block body %}
    <h1>Modifier {{ pokemon.nom }}</h1>

    {{ include('pokemon/_form.html.twig', {'button_label': 'Valider', 'class': 'primary'}) }}

    <a class="btn btn-secondary" href="{{ path('pokemon_index') }}">Retour à la liste des pokemons</a>

    {{ include('pokemon/_delete_form.html.twig') }}
{% endblock %}
